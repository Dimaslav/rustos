[package]
name = "myos"
version = "0.1.0"
edition = "2021"

[dependencies]

[build-dependencies]
bootloader = "0.11.17"

[features]
# Пробрасывается в build.rs → kernel. Прогон smoke-тестов без GUI.
headless_test = []

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"